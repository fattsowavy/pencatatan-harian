name: penghitung_harian
description: A Flutter app for tracking daily expenses.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  crypto: ^3.0.6
  provider: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/onboarding1.png
